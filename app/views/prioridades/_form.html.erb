<%= simple_form_for(@prioridad, :html => { :class => 'form-horizontal' }, :remote => true  ) do |f| %>
  <%= f.input :nombre %>

  <div class="form-actions">
    <%= f.submit "Guardar", :class => 'btn btn-primary' %>
    <%= link_to 'Atras', prioridades_path, :class => 'btn btn-default' %>
  </div>
<% end %>
