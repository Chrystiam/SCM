<%- model_class = Falta -%>
<br><br>
<h1>Faltas</h1>
<br>
<%= form_tag request.path, :method => 'get' do %>
  <%= content_tag :label do %>
    falta:
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Consultar", :falta => nil %>
  <% end %>
<% end %>

<%= form_tag request.path, :method => 'get' do %>
  <%= content_tag :label do %>
    Numero Registros:
    <%= text_field_tag :registro, params[:registro], :class =>'input-mini' %>
   <% end %>
<% end %>

<p>
Total Faltas : <%= @faltas.total_entries %>
</p>

<table class="table table-bordered table-hover pretty">
  <thead>
    <tr>
      <th><%= sortable "id" %></th>
      <th><%= sortable "falta" %></th>
      <th><%= sortable "descripcion" %></th> 
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <%= render @faltas %>
  </tbody>
</table>
<%= link_to 'Nuevo Documento', new_falta_path, :class => 'btn btn-primary', :remote => true %>

<div align="center">
  <%= will_paginate @faltas, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente
  &raquo;' %>
</div>

